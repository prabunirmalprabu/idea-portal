// Deprecated: the admin timeline now uses components/TimelineTable.js instead
// of a Gantt chart (gantt-task-react has been removed from package.json).
// The Gantt view could never reliably fit the page without a scrollbar or
// cramped columns, so it was replaced with a filterable, sortable table.
// This file is kept only as an unused stub since it can't be deleted from
// this workspace.
export {};
